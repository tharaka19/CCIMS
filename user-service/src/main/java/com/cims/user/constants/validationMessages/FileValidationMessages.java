package com.cims.user.constants.validationMessages;

/**
 * The {@code FileValidationMessages} class contains constants for file validation messages.
 * These messages are used by the application to validate file information provided by the user.
 * The constants are used as keys to retrieve the corresponding message from the resource bundle and
 * display them to the user as feedback for validation errors.
 */
public class FileValidationMessages {
    public static final String EMPTY_FILE_TYPE = "No files were uploaded.";
    public static final String LARGE_FILE = "File size is too large.";
    public static final String INVALID_FILE_TYPE = "Invalid file type.";
}
