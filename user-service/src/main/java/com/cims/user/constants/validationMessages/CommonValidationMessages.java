package com.cims.user.constants.validationMessages;

/**
 * The {@code CommonValidationMessages} class contains constants for common validation messages.
 * These messages are used by the application to validate common information provided by the user.
 * The constants are used as keys to retrieve the corresponding message from the resource bundle and
 * display them to the user as feedback for validation errors.
 */
public class CommonValidationMessages {
    public static final String EMPTY_STATUS = "Please select a status.";
}
